{
  "workflow": "cinema-machina-brain-cycle",
  "description": "Screenpipe -> Qwen capsule -> Graphify context -> agent pack -> Langflow bridge",
  "entrypoint": "tools/brain-ops/run_brain_cycle.sh",
  "tasks": [
    {
      "name": "brain_cycle",
      "command": "bash tools/brain-ops/run_brain_cycle.sh"
    }
  ],
  "notes": [
    "This is a truthful workspace config footprint for RuFlo orchestration.",
    "If your RuFlo CLI uses a different schema, adapt this config to its native format."
  ]
}
